const {renderLicenseBadge, renderLicenseLink, renderLicenseSection} = require('./licensure');
const renderLiveLink = require(`./`);
// const renderTechnology = require(`./technologies`)
const {HTMLanswers, CSSanswers, JSanswers} = require(`./`);
const {renderImages, renderVideo} = require(`./`);
const contributionsOrNah = require(``);

//a function to generate markdown for README
function generateHTML(answers) {
  console.log(answers)
  return  
};

module.exports = generateHTML;